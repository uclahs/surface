<h{{ title.level|default('2') }} class="heading{{ title.modifier ? ' ' ~ title.modifier }}">
  {% if title.url %}
    <a href="{{ title.url }}" class="heading__link">
      {{ title.text }}
    </a>
  {% else %}
    {{ title.text }}
  {% endif %}
</h{{ title.level|default('2') }}>
