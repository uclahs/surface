<?php

/**
 * Implements hook_theme_suggestions_HOOK_alter().
 */
function surface_theme_suggestions_user_alter(&$suggestions, $variables) {
  if (isset($variables['elements']['#view_mode'])) {
    $suggestions[] = 'user__' . $variables['elements']['#view_mode'];
  }
}
