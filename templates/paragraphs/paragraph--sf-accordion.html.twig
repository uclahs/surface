{# Set variable to trigger rendering of content array. #}
{% set rendered_content = content|render %}

{% include '@components/accordion/accordion.twig' with {
  'attributes': attributes,
  'id': paragraph.id(),
  'title': content.field_sf_title|field_value,
  'text': content.field_sf_wysiwyg|field_value,
} only %}
